RootVault -> ERC20, Subvaults[]

Subvaults[0] == ERC20Vault
Subvaults[1..n] = IntegrationVault[1..n]

Subvault functions:
    push
    pull
    tvl

Subvaults:
    ERC20Vault
    DefaultBondVault
    TBD

RootVault functions:
    deposit
    externalCall
    tvl
    registerWithdrawal
    instantWithdrawal


Первый волт - однотокенный. DefaultBondVault - единственный токен (steth). Ничего не делаем с этим бондом.
Для Symbiotic StakingCollector-контрактов нужены будут волты,
Каждый из них наследуется от одного из конкретного из бондов (формально бонд - это просто ERC20 токен). Наверное можно сделать рекурсивную имплементацию
ERC20Vault.asset() = self
BondVault.asset() = ERC20 token
StakingCollectorVault.asset() = Bond token


